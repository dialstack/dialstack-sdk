/**
 * `@dialstack/sdk/react/core` — the headless, framework-agnostic React core the
 * SDK's platform packages build on (DOM/RN-free). Today it re-exports the
 * softphone core; future shared cores (e.g. a dialer core) re-export here too.
 *
 * @packageDocumentation
 */

export * from './softphone-core';
