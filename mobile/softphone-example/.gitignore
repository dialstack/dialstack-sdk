# Dependencies
node_modules/

# Expo / Metro
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects generated by `expo prebuild` — never commit (also keeps them
# out of the public SDK mirror).
/ios/
/android/

# Misc
*.log
.DS_Store

# Local dev session (token) - never commit
dev-session.local.json
