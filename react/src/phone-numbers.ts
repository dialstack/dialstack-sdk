/**
 * `<PhoneNumbers>` — the phone-number inventory table.
 *
 * Its own entry point; needs an initialized instance via
 * `<DialstackComponentsProvider>` from `@dialstack/sdk-react`.
 *
 * @packageDocumentation
 */

export { PhoneNumbers } from './react/PhoneNumbers';
export type { PhoneNumbersProps } from './react/PhoneNumbers';
