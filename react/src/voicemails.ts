/**
 * `<Voicemails>` — the voicemail list.
 *
 * Its own entry point; needs an initialized instance via
 * `<DialstackComponentsProvider>` from `@dialstack/sdk-react`.
 *
 * @packageDocumentation
 */

export { Voicemails } from './react/Voicemails';
export type { VoicemailsProps } from './react/Voicemails';
