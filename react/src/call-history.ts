/**
 * `<CallHistory>` — per-contact call history.
 *
 * Its own entry point; needs an initialized instance via
 * `<DialstackComponentsProvider>` from `@dialstack/sdk-react`.
 *
 * @packageDocumentation
 */

export { CallHistory } from './react/CallHistory';
export type { CallHistoryProps } from './react/CallHistory';
