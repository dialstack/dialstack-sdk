/**
 * `<PhoneNumberOrdering>` — search and order new numbers.
 *
 * Its own entry point; needs an initialized instance via
 * `<DialstackComponentsProvider>` from `@dialstack/sdk-react`.
 *
 * @packageDocumentation
 */

export { PhoneNumberOrdering } from './react/PhoneNumberOrdering';
export type { PhoneNumberOrderingProps } from './react/PhoneNumberOrdering';
